{
  "name": "sendly-test-repo",
  "private": true,
  "scripts": {
    "test": "npm run test:js && npm run test:py",
    "test:js": "node --test",
    "test:py": "python3 -m unittest discover -s . -p \"test_*.py\""
  }
}
